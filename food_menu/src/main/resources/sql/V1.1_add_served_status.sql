-- =====================================================
-- 餐次订单新增"已出餐"状态
-- 版本: V1.1
-- 创建时间: 2025-12-19
-- 说明: 更新 daily_meal_order 表的 status 字段注释
-- =====================================================

-- 更新 status 字段注释，新增状态3(已出餐)
ALTER TABLE `daily_meal_order` MODIFY COLUMN `status` TINYINT NOT NULL DEFAULT 0 COMMENT '状态: 0-收集中, 1-已确认, 2-已截止, 3-已出餐';

-- =====================================================
-- 迁移完成
-- =====================================================
